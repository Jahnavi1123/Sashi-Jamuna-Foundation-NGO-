OLD HTML DOCUMENTS - OPTIONAL ARCHIVE

These are the four original HTML files, preserved with their original names:
1.html
3.html
index.html
index1.html

The current website and admin do not load anything from this folder.
Deleting this entire folder does not change or break the active website.

conversion-tools contains historical conversion helpers only. They are not
required to view, run, edit or test the current website. Manually running the
archived build_site.py would regenerate and overwrite the active website;
normally, edit the active website files directly instead.

previous-integration contains the earlier blue public-site implementation
before the public design was corrected to match 1.html. It is also optional.
